name: tests

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  tests:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install
        # mcp + app extras so the local-tools and GitHub App surfaces are tested
        # too; provider SDKs (openai/gemini) stay out — their adapters are tested
        # via the missing-SDK path.
        run: pip install -e ".[dev,mcp,app]"
      - name: Lint
        run: ruff check .
      - name: Type-check
        run: mypy
      # No ANTHROPIC_API_KEY: the suite runs fully mocked (constitution: CI
      # tests must pass without live API credentials).
      - name: Test
        run: pytest
